# Implementation Plans

Generated by the improve skill on 2026-06-21 at commit `b8dfd95`. Execute in the order below unless dependencies say otherwise. Each executor should read the plan fully before starting, honor its STOP conditions, and update its row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Characterize analytics query outputs before optimizing them | P1 | M | - | DONE |
| 002 | Make the creatives list query set-based and avoid initial health rollup | P1 | M | 001 | DONE |
| 003 | Simplify daily performance SQL for canonical daily rows | P1 | S-M | 001 | DONE |
| 004 | Defer hidden analytics and split MER first-load data | P2 | M | 001, 003 | DONE |
| 005 | Verify and add indexes for canonical performance-log analytics | P2 | S | 002, 003 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 001 comes first because the user specifically wants faster queries without breaking current numbers or response shapes.
- 002 depends on 001 because it rewrites the riskiest query shape on the creatives page.
- 003 depends on 001 because the date-series simplification can subtly change chart rows if not characterized first.
- 004 depends on 003 because MER still needs daily chart and account breakdown data on first load; making those cheaper should happen before retiming secondary queries.
- 005 comes last because indexes should be chosen against the optimized SQL, not against query shapes that are about to change.

## Findings considered and rejected

- Duplicate role/org lookup on first load: rejected for now because `AppSidebar` and `useActiveOrganizationRole` use the same React Query key, `["org-full", orgId]`, so this is likely deduped and lower leverage than analytics SQL.
- Server-rendered tRPC prefetch: rejected for now because the audited overview, creatives, and MER routes are client components. Moving them to SSR is a larger architecture change and not required for the first performance pass.
- Separate plan for `dashboardStats` sequential internals: deferred. Plan 004 reduces where full `dashboardStats` is needed on first load, and Plan 005 verifies remaining hot SQL with `EXPLAIN`. If `dashboardStats` remains slow after those, create a focused follow-up plan.
- Blindly adding indexes first: rejected. Current schema has broad indexes, but the best new index depends on the post-refactor SQL and must be verified with `EXPLAIN`.
